Architecture Decision Records (ADRs) 001–005Project: U.S. Socioeconomic, Political & Agricultural Trends Platform
Status: Approved
Last Updated: 2026-08-06ADR-001: Database Selection — PostgreSQL + DuckDBDate: 2026-08-06
Status: Accepted
Context: The platform requires a primary operational database supporting transactional writes, versioned data retrieval, and historical governance, plus an analytics engine capable of efficient columnar queries over time-series data without requiring separate infrastructure.Decision: Adopt PostgreSQL as the primary operational database and DuckDB as the dedicated analytics engine.Rationale:RequirementPostgreSQLDuckDBTrade-offTransactional integrity ACID, proven Not designed for thisPostgreSQL primary responsibilityVersioned dataset storage Excellent with proper schema—Operational queries (API, UI live data) Optimized for OLTP Not optimizedPostgreSQL onlyAnalytical queries (CAGR, aggregations, time-series) Works, but not optimized Vectorized columnar OLAPDuckDB for analyticsNo infrastructure overhead Single process In-process or embeddedBoth excellentOpen source PostgreSQL License MITBoth fully FOSSCost Free FreeBoth $0Vendor lock-in None NonePortable everywhereArchitecture:PostgreSQL (Primary)├── Raw observation storage (immutable)├── Dataset lifecycle governance├── Provenance + transformation logs├── Metadata + metrics definitions└── API query layer ↓ (Export to Parquet on schedule)DuckDB (Analytics)├── Read Parquet snapshots├── Compute CAGR, ratios, per-capita values├── Index-normalized comparisons├── Time-series aggregations└── Export derived results → PostgreSQL (immutable, labeled "Modeled/Derived")Consequences:  Positive:PostgreSQL handles all transactional governance; DuckDB never touches production dataDerived analytics are computed in isolation, versioned, never overwriting raw observationsZero infrastructure complexity for MVPTrivial to scale DuckDB to external queries if needed (HTTPS endpoint)Both tools are self-hostable on any Linux VPS  Negative:Two databases to version-upgrade (manageable; both stable projects)Data movement between PostgreSQL → Parquet → DuckDB adds a scheduling dependency (APScheduler is lightweight, not a burden)DuckDB results must be validated before re-import to PostgreSQL as "Modeled/Derived" tier (this is intentional governance, not a defect)Alternatives Considered:AlternativeRejected BecausePostgreSQL onlyColumnar queries on massive time-series become slow; users blocked waiting for aggregationsMongoDBNot suitable for strict, versioned, provenance-tracked data; schema-less is a liability hereTimescaleDBAdds complexity; PostgreSQL + DuckDB gives us the same benefit with fewer moving partsElasticsearchOverkill for MVP; PostgreSQL FTS sufficient for Phase F searchBigQuery / SnowflakeProprietary SaaS; creates vendor lock-in; contradicts FOSS/self-hosted goalMigration Path:If future use cases require a data warehouse (Phase G+), switching to a proper DW (e.g., Apache Iceberg + Spark) is possible without redesigning the operational layer. PostgreSQL stays as-is.Related ADRs: ADR-002 (data ingestion), ADR-003 (immutable storage)ADR-002: Data Storage Strategy — Immutable Raw + Versioned TransformsDate: 2026-08-06
Status: Accepted
Context: The Master Project Plan §6 and §22 mandate that raw observations remain immutable and that transformations are versioned separately. The platform must preserve complete provenance chains and enable reproducibility. Data ingestion, validation, and publication must follow a clear lifecycle without silent overwrites.Decision: Implement a three-tier data storage strategy:Immutable Raw Layer — Original retrieved observations, never modifiedStandardized Layer — Unit-normalized, date-harmonized data (distinct from raw)Analytics Layer — Derived metrics, clearly labeled "Modeled/Derived"Rationale:Authoritative Source (FRED, BLS, Census, USDA, etc.) ↓ [Ingestion] ↓PostgreSQL: raw_observations├── source_id├── dataset_id├── retrieval_timestamp ← Immutable record of when fetched├── source_checksum ← Hash of original file├── raw_value├── raw_units├── raw_date└── metadata (never overwritten) ↓ [Validation & Standardization] ↓PostgreSQL: standardized_observations├── raw_observation_id ← Links back to immutable├── standardized_value ← Units converted (e.g., USD 2012=100)├── standardized_units├── standardized_date ← ISO 8601├── inflation_adjusted_to├── confidence_tier ← "Official Measurement" / "Survey Estimate" / etc.└── transformation_id ← Links to versioned transformation script ↓ [Analytics Engine (DuckDB)] ↓PostgreSQL: derived_observations├── standardized_observation_ids ← Multiple inputs (e.g., CAGR over 10-yr period)├── derived_value├── derivation_formula ← Reproducible: CAGR = ((End/Start)^(1/n))-1├── derivation_timestamp├── confidence_tier = "Modeled/Derived"└── methodology_reference_id ← Links to documented methodologyWhy Three Layers:LayerPurposeMutabilityConsumerRawHistorical record; audit trail; reproducibilityImmutable foreverData engineers (validation)StandardizedCross-domain comparisons; unit consistencyAppend-only (versioned replacements only)API queries, frontendDerivedUser-requested analyticsComputed on-demand or cachedAPI analytics endpointsImmutability Rules:python# Raw observations: NEVER UPDATEINSERT INTO raw_observations (...) VALUES (...); # UPDATE raw_observations SET raw_value = ...; # FORBIDDEN# Standardized observations: APPEND ONLY (new versions, not overwrites)INSERT INTO standardized_observations ( raw_observation_id, transformation_id, # v1 → v2 → v3 of transformation pipeline standardized_value, valid_from, # When this version became current valid_to # NULL if current; populated if superseded); # # Derived observations: COMPUTED, NOT STORED (or cached with TTL)SELECT CAGR(...) FROM standardized_observations WHERE ...; # Consequences:  Positive:Complete audit trail; can reproduce any historical published metricSilent data corruption is impossibleTransformation scripts are versioned; can re-run on old dataRevisions are explicit, not hidden (Master Plan §21)Supports regulatory compliance + reproducible research  Negative:Database grows over time (mitigated: compress raw layer periodically; archive snapshots to Parquet)Queries must understand layer semantics (mitigated: API abstracts this; users don't care)No single "current" view without explicit filtering on valid_to IS NULL (mitigated: views hide complexity)Data Lifecycle (Master Plan §21):Identified ↓Registered (entered into source registry) ↓Retrieved (inserted into raw_observations, with retrieval_timestamp) ↓Validated (schema + provenance checks pass; validation_id recorded) ↓Standardized (inserted into standardized_observations, transformation_id recorded) ↓Approved (manual review; approval_id recorded; quality gate passed) ↓Published (exposed via API; marked public=true) ↓Revised (if upstream source changes: new retrieval → new standardized row) (old standardized rows marked valid_to=now; API shows latest)Revision Handling Example:BLS revises employment data from July 2023 (discovered error in survey methodology):sql-- Old standardized observation marked supersededUPDATE standardized_observations SET valid_to = NOW(), revision_reason = 'BLS revision notice 2026-08-01'WHERE dataset_id = 'BLS_CE' AND metric = 'total_nonfarm_employment' AND observation_date = '2023-07-01' AND valid_to IS NULL;-- New standardized observation insertedINSERT INTO standardized_observations (...)VALUES ( raw_observation_id = <new retrieval>, standardized_value = 157450000, -- Revised figure valid_from = NOW(), valid_to = NULL, revision_reason = 'BLS revision notice 2026-08-01', transformation_id = 'transform_v2');-- API automatically shows revised value (WHERE valid_to IS NULL)-- Historical record preserved; revision is visible via audit endpointStorage Estimates (10 years):Raw layer: ~500 MB–1 GB (compressed, original files + metadata)Standardized layer: ~2–5 GB (normalized observations)Derived analytics: Computed on-demand or cached in Redis (transient)Related ADRs: ADR-001 (databases), ADR-003 (ingestion), ADR-004 (provenance)ADR-003: Data Ingestion Architecture — Source-Specific ConnectorsDate: 2026-08-06
Status: Accepted
Context: The platform must ingest data from ~15 authoritative government sources (Census, BLS, BEA, FRED, USDA, USCIS, DHS, CMS, CDC, NCES, etc.), each with different APIs, update frequencies, data structures, and licensing terms. A monolithic ingestion layer will become unmaintainable; source-specific connectors must isolate API quirks and enable independent testing, versioning, and governance.Decision: Implement a modular, source-specific connector pattern using a shared ingestion framework:backend/app/ingestion/├── __init__.py├── base.py # Abstract SourceConnector base class├── connectors/│ ├── census.py # U.S. Census Bureau (Decennial + ACS APIs)│ ├── bls.py # Bureau of Labor Statistics (HTTPS + FTP)│ ├── bea.py # Bureau of Economic Analysis (NIPA tables)│ ├── fred.py # Federal Reserve (FRED API)│ ├── cbo.py # Congressional Budget Office (CSV downloads)│ ├── treasury.py # Dept of Treasury (manual CSV/PDF)│ ├── usda_nass.py # USDA NASS (Quick Stats API)│ ├── usda_ers.py # USDA ERS (download + API)│ ├── usda_rma.py # USDA RMA (crop insurance data)│ ├── uscis.py # USCIS (annual reports, limited API)│ ├── dhs.py # DHS (border, immigration statistics)│ ├── cms.py # CMS (healthcare spending)│ ├── cdc.py # CDC (vital statistics, epidemiology)│ ├── nces.py # NCES (education)│ ├── irs.py # IRS (tax statistics)│ ├── ssa.py # Social Security Administration│ └── worldbank.py # World Bank (for international comparisons)└── scheduler.py # APScheduler job orchestrationBase Connector Class (Framework):pythonfrom abc import ABC, abstractmethodfrom datetime import datetimeimport hashlibclass SourceConnector(ABC): """Abstract base for all source-specific connectors.""" def __init__(self, source_id: str, config: dict): self.source_id = source_id self.config = config @abstractmethod def fetch(self, dataset_id: str, params: dict) -> list[dict]: """Fetch raw data from source API/download.""" pass @abstractmethod def validate_schema(self, raw_data: list[dict]) -> bool: """Validate retrieved data matches expected schema.""" pass def compute_checksum(self, raw_data: list[dict]) -> str: """Hash the retrieved payload for integrity.""" payload = str(raw_data).encode() return hashlib.sha256(payload).hexdigest() def store_raw( self, dataset_id: str, raw_data: list[dict], retrieval_timestamp: datetime, checksum: str ) -> int: """Insert into raw_observations layer (immutable).""" # Implementation inserts to PostgreSQL pass def orchestrate(self, dataset_id: str) -> dict: """ Full ingestion workflow: fetch() → validate_schema() → compute_checksum() → store_raw() Returns status report for logging/alerting. """ try: raw_data = self.fetch(dataset_id, self.config.get('params', {})) if not self.validate_schema(raw_data): return {'status': 'FAILED', 'reason': 'Schema validation failed'} checksum = self.compute_checksum(raw_data) retrieval_timestamp = datetime.utcnow() raw_id = self.store_raw( dataset_id, raw_data, retrieval_timestamp, checksum ) return { 'status': 'SUCCESS', 'raw_observation_ids': [raw_id], 'row_count': len(raw_data), 'checksum': checksum, 'retrieval_timestamp': retrieval_timestamp.isoformat() } except Exception as e: return {'status': 'FAILED', 'reason': str(e), 'exception': type(e).__name__}Concrete Example — BLS Connector:pythonimport requestsfrom datetime import datetimeclass BLSConnector(SourceConnector): """Bureau of Labor Statistics connector.""" API_BASE = "https://api.bls.gov/publicAPI/v2/" def fetch(self, dataset_id: str, params: dict) -> list[dict]: """ dataset_id: e.g., 'BLS_CE' (Current Employment Statistics) params: {'series_id': 'CES0000000001', 'start_year': 1990, ...} """ response = requests.post( self.API_BASE + "timeseries/data", json={ 'seriesid': params.get('series_id'), 'startyear': params.get('start_year'), 'endyear': params.get('end_year'), 'registrationkey': self.config['api_key'] }, timeout=30 ) response.raise_for_status() data = response.json() if data['status'] != 'REQUEST_SUCCEEDED': raise Exception(f"BLS API error: {data.get('message')}") # Transform BLS API response to standardized raw format observations = [] for series in data['Results']['series']: for data_point in series['data']: observations.append({ 'series_id': series['seriesID'], 'period': data_point['period'], 'year': data_point['year'], 'value': float(data_point['value']), 'footnotes': data_point.get('footnotes', '') }) return observations def validate_schema(self, raw_data: list[dict]) -> bool: """Ensure all expected fields present.""" required_fields = {'series_id', 'period', 'year', 'value'} for obs in raw_data: if not required_fields.issubset(obs.keys()): return False return len(raw_data) > 0Scheduler Orchestration (APScheduler):pythonfrom apscheduler.schedulers.background import BackgroundSchedulerfrom apscheduler.triggers.cron import CronTriggerimport loggingclass IngestionScheduler: def __init__(self, db_session): self.scheduler = BackgroundScheduler() self.db = db_session self.logger = logging.getLogger(__name__) def register_job(self, source_id: str, dataset_id: str, schedule: str): """ Register a scheduled ingestion job. schedule: cron format, e.g., '0 8 * * MON' (every Monday 8 AM UTC) """ connector_class = CONNECTORS[source_id] # Import map connector = connector_class(source_id, CONFIG[source_id]) job = self.scheduler.add_job( func=self._run_ingestion, trigger=CronTrigger.from_crontab(schedule), args=[source_id, dataset_id, connector], id=f"{source_id}_{dataset_id}", name=f"Ingest {dataset_id} from {source_id}", misfire_grace_time=600 # Allow 10-min jitter ) self.logger.info(f"Registered job: {job.id}") def _run_ingestion(self, source_id: str, dataset_id: str, connector): """Execute ingestion; log success/failure.""" self.logger.info(f"Starting ingestion: {source_id}/{dataset_id}") result = connector.orchestrate(dataset_id) if result['status'] == 'SUCCESS': self.logger.info( f" {source_id}/{dataset_id}: " f"{result['row_count']} rows, checksum={result['checksum'][:8]}" ) else: self.logger.error( f" {source_id}/{dataset_id}: {result['reason']}" ) # Alert ops (email, Slack, etc.) def start(self): self.scheduler.start()Connector Registry (Configuration):yaml# config/ingestion.yamlsources: census: connector: CensusConnector datasets: - id: CENSUS_DECENNIAL_2020 schedule: "0 12 1 1 *" # Annual, Jan 1 noon UTC params: api_key: ${CENSUS_API_KEY} - id: CENSUS_ACS_5YR_2023 schedule: "0 12 1 9 *" # Annual, Sept 1 noon UTC bls: connector: BLSConnector datasets: - id: BLS_CE schedule: "0 8 1 * FRI" # First Friday monthly params: series_id: CES0000000001 start_year: 1990 - id: BLS_LNU schedule: "0 8 1 * FRI" fred: connector: FREDConnector datasets: - id: FRED_GDP schedule: "0 14 * * THU" # Every Thursday 2 PM UTC params: series_id: GDPConsequences:  Positive:Each connector independently testable; mock source APIs in unit testsSource-specific quirks isolated; changes to one API don't rippleConnectors can be versioned separately (e.g., bls.py v2 if API changes)New sources added without touching existing connectorsComplete audit trail: every ingestion logged with connector version, timestamp, checksumFailed ingestions block standardization gate; users never see bad data  Negative:15–20 connector files to maintain (mitigated: base class handles 80% of boilerplate)Scheduling complexity (mitigated: APScheduler is lightweight + battle-tested)Source API changes require connector maintenance (unavoidable; document as ongoing)Error Handling & Resilience:python# If BLS API returns 429 (rate limit), retry with exponential backofffrom requests.adapters import HTTPAdapterfrom urllib3.util.retry import Retrysession = requests.Session()retry_strategy = Retry( total=3, backoff_factor=1, # 1s, 2s, 4s status_forcelist=[429, 500, 502, 503, 504])adapter = HTTPAdapter(max_retries=retry_strategy)session.mount("http://", adapter)session.mount("https://", adapter)Related ADRs: ADR-002 (storage), ADR-004 (provenance)ADR-004: Provenance & Transformation TrackingDate: 2026-08-06
Status: Accepted
Context: Master Plan §20 requires complete traceability: Observation → Metric → Dataset → Series → Agency → Retrieval → Transformation → Visualization. The platform must be reproducible; every published metric must link back to its source and documented transformation logic. Transformation scripts themselves must be versioned so analysts can understand exactly what happened to a number.Decision: Implement a provenance tracking layer recording the complete lineage of every observation through the pipeline.Database Schema — Provenance Entities:sql-- Records external data sources (agency + API/download method)CREATE TABLE sources ( source_id UUID PRIMARY KEY, agency_name VARCHAR NOT NULL, -- "Bureau of Labor Statistics" dataset_name VARCHAR NOT NULL, -- "Current Employment Statistics" api_endpoint VARCHAR, -- "https://api.bls.gov/publicAPI/v2/" documentation_url VARCHAR, -- Link to agency documentation license TEXT, -- "Public Domain" / CC-BY / etc. update_frequency VARCHAR, -- "monthly", "quarterly", "annual" created_at TIMESTAMP DEFAULT NOW(), created_by VARCHAR, last_updated_at TIMESTAMP DEFAULT NOW());-- Ingestion events: when data was fetched and stored rawCREATE TABLE ingestion_events ( ingestion_event_id UUID PRIMARY KEY, source_id UUID REFERENCES sources(source_id), dataset_id VARCHAR NOT NULL, retrieval_timestamp TIMESTAMP NOT NULL, -- When fetched from source retrieval_method VARCHAR NOT NULL, -- "API_GET", "CSV_DOWNLOAD", "FTP", "MANUAL" source_version VARCHAR, -- If source reports version/revision http_status_code INT, -- 200, 429, 500, etc. row_count INT, file_size_bytes BIGINT, file_checksum VARCHAR NOT NULL, -- SHA256 of raw payload error_message TEXT, -- If retrieval failed created_at TIMESTAMP DEFAULT NOW());-- Transformation scripts: versions of the standardization/analytics logicCREATE TABLE transformation_scripts ( transformation_script_id UUID PRIMARY KEY, script_name VARCHAR NOT NULL, -- "standardize_unemployment_rate" script_version VARCHAR NOT NULL, -- semver: 1.0.0, 1.1.0, etc. script_language VARCHAR, -- "python", "sql" script_hash VARCHAR NOT NULL, -- SHA256(script code) for integrity script_source_url VARCHAR, -- Link to GitHub/repo description TEXT, -- What the transformation does input_schema JSON, -- Expected input fields output_schema JSON, -- Output fields after transform created_at TIMESTAMP NOT NULL, created_by VARCHAR NOT NULL, approved_at TIMESTAMP, -- When manually reviewed approved_by VARCHAR, deprecated_at TIMESTAMP -- If superseded);-- Each transformation applied to raw data leaves a recordCREATE TABLE transformations ( transformation_id UUID PRIMARY KEY, raw_observation_id UUID NOT NULL, -- Input: raw observation transformation_script_id UUID REFERENCES transformation_scripts(transformation_script_id), standardized_observation_id UUID, -- Output: standardized observation applied_at TIMESTAMP DEFAULT NOW(), applied_by VARCHAR, -- User/service that ran transformation parameters JSON, -- Params passed to script (e.g., inflation_basis_year) computation_time_ms INT, -- How long transformation took validation_passed BOOLEAN DEFAULT TRUE);-- Observations flow through multiple standardization stepsCREATE TABLE standardized_observations ( standardized_observation_id UUID PRIMARY KEY, raw_observation_id UUID NOT NULL, metric_id UUID REFERENCES metrics(metric_id), dataset_id VARCHAR NOT NULL, series_id VARCHAR NOT NULL, observation_date DATE NOT NULL, standardized_value NUMERIC, standardized_units VARCHAR, inflation_adjusted_to INT, -- e.g., 2012 (base year) confidence_tier VARCHAR NOT NULL, -- "Official Measurement", "Survey Estimate", etc. transformation_id UUID REFERENCES transformations(transformation_id), created_at TIMESTAMP DEFAULT NOW(), valid_from TIMESTAMP DEFAULT NOW(), valid_to TIMESTAMP, -- NULL if current; set if revised revision_reason VARCHAR, -- Why this observation was replaced approved_at TIMESTAMP, approved_by VARCHAR);-- Derived (analytics) observations record their lineageCREATE TABLE derived_observations ( derived_observation_id UUID PRIMARY KEY, standardized_observation_ids UUID[], -- Input observations (e.g., CAGR uses 2+) metric_id UUID REFERENCES metrics(metric_id), derivation_formula VARCHAR NOT NULL, -- "CAGR = ((End/Start)^(1/n))-1" derivation_script_id UUID REFERENCES transformation_scripts(transformation_script_id), observation_period_start DATE, observation_period_end DATE, derived_value NUMERIC, confidence_tier VARCHAR DEFAULT 'Modeled/Derived', methodology_reference_id VARCHAR, -- Link to methodology doc created_at TIMESTAMP DEFAULT NOW(), valid_from TIMESTAMP, valid_to TIMESTAMP);-- Historical events (context, not causal)CREATE TABLE historical_events ( event_id UUID PRIMARY KEY, title VARCHAR NOT NULL, event_date DATE NOT NULL, category VARCHAR, -- "War", "Economic Crisis", "Legislation", etc. description TEXT, affected_domain_ids VARCHAR[], -- Which domains this event touches primary_source_url VARCHAR, confidence_tier VARCHAR, created_at TIMESTAMP DEFAULT NOW());Provenance Display — API Endpoint:python# Endpoint: GET /api/v1/observations/{observation_id}/provenance@router.get('/observations/{observation_id}/provenance')async def get_provenance(observation_id: UUID) -> ProvenanceChain: """ Return the complete lineage chain for an observation. """ obs = db.query(StandardizedObservation).filter_by( standardized_observation_id=observation_id ).one() raw_obs = db.query(RawObservation).filter_by( raw_observation_id=obs.raw_observation_id ).one() ingestion = db.query(IngestionEvent).filter_by( ingestion_event_id=raw_obs.ingestion_event_id ).one() source = db.query(Source).filter_by( source_id=ingestion.source_id ).one() transformation = db.query(Transformation).filter_by( transformation_id=obs.transformation_id ).one() script = db.query(TransformationScript).filter_by( transformation_script_id=transformation.transformation_script_id ).one() return ProvenanceChain( observation_id=observation_id, value=obs.standardized_value, units=obs.standardized_units, confidence_tier=obs.confidence_tier, chain=[ { 'step': 1, 'stage': 'SOURCE', 'agency': source.agency_name, 'dataset': source.dataset_name, 'documentation': source.documentation_url, 'license': source.license }, { 'step': 2, 'stage': 'RETRIEVAL', 'timestamp': ingestion.retrieval_timestamp.isoformat(), 'method': ingestion.retrieval_method, 'checksum': ingestion.file_checksum, 'row_count': ingestion.row_count }, { 'step': 3, 'stage': 'TRANSFORMATION', 'script': script.script_name, 'version': script.script_version, 'parameters': transformation.parameters, 'applied_at': transformation.applied_at.isoformat(), 'computation_time_ms': transformation.computation_time_ms }, { 'step': 4, 'stage': 'STANDARDIZATION', 'inflation_basis': obs.inflation_adjusted_to, 'approval_status': 'APPROVED' if obs.approved_at else 'PENDING' } ] )Frontend Provenance Display Component:tsx// components/provenance/ProvenanceChain.tsxexport function ProvenanceChain({ observationId }: Props) { const { data: provenance, isLoading } = useQuery( ['provenance', observationId], () => fetchProvenance(observationId) ); if (isLoading) return <Skeleton />; return ( <div className="provenance-chain"> <h3 className="font-bold text-sm">Data Provenance</h3> {provenance.chain.map((step) => ( <div key={step.step} className="provenance-step"> <div className="step-number">{step.step}</div> <div className="step-content"> <h4 className="font-semibold">{step.stage}</h4> {step.stage === 'SOURCE' && ( <div className="details"> <p><strong>Agency:</strong> {step.agency}</p> <p><strong>Dataset:</strong> {step.dataset}</p> <p><strong>License:</strong> {step.license}</p> <a href={step.documentation} target="_blank" rel="noopener"> Documentation → </a> </div> )} {step.stage === 'RETRIEVAL' && ( <div className="details"> <p><strong>Retrieved:</strong> {step.timestamp}</p> <p><strong>Method:</strong> {step.method}</p> <p><strong>Checksum:</strong> <code>{step.checksum.slice(0, 16)}…</code></p> <p><strong>Rows:</strong> {step.row_count.toLocaleString()}</p> </div> )} {step.stage === 'TRANSFORMATION' && ( <div className="details"> <p><strong>Script:</strong> {step.script} v{step.version}</p> <p><strong>Applied:</strong> {step.applied_at}</p> <p><strong>Time:</strong> {step.computation_time_ms} ms</p> <details> <summary>Parameters</summary> <pre>{JSON.stringify(step.parameters, null, 2)}</pre> </details> </div> )} {step.stage === 'STANDARDIZATION' && ( <div className="details"> <p><strong>Inflation Basis:</strong> {step.inflation_basis}</p> <p><strong>Status:</strong> {step.approval_status}</p> </div> )} </div> {step.step < provenance.chain.length && ( <div className="arrow-down">↓</div> )} </div> ))} </div> );}Consequences:  Positive:Every observation reproducible; can re-run transformation against raw data if script changesSilent data corruption detected (checksum mismatch alert)Revision auditing explicit; can see when/why/by-whom a number changedTrust: users can verify a metric's origin without trusting the platformRegulatory compliance: complete CRISPR trail for data governance  Negative:Database overhead (mitigated: indexes on frequent queries; archive old provenance)Complexity for API consumers (mitigated: most users just follow one link, get context)Related ADRs: ADR-002 (storage), ADR-003 (ingestion)ADR-005: Confidence Tiers & Missing Data RepresentationDate: 2026-08-06
Status: Accepted
Context: Master Plan §18 defines six confidence tiers (Official Measurement, Administrative Record, Survey Estimate, Historical Reconstruction, Academic Estimate, Modeled/Derived, Unknown). Every observation must be labeled. Missing data must never be represented as zero or omitted silently; it must communicate why data is unavailable. The platform is trustworthy only if users can distinguish certainty from speculation.Decision: Implement a mandatory confidence-tier system with explicit missing-data states and frontend visualization.Confidence Tier Definitions & Database Schema:sqlCREATE TYPE confidence_tier AS ENUM ( 'OFFICIAL_MEASUREMENT', -- Direct measurement by authoritative agency 'ADMINISTRATIVE_RECORD', -- Derived from government admin processes 'SURVEY_ESTIMATE', -- Sampled government survey (e.g., CPS) 'HISTORICAL_RECONSTRUCTION', -- Reconstructed from historical documents 'ACADEMIC_ESTIMATE', -- Published peer-reviewed research 'MODELED_DERIVED', -- Calculated from other observations 'UNKNOWN' -- Cannot be classified);CREATE TABLE confidence_tier_definitions ( tier_id confidence_tier PRIMARY KEY, display_name VARCHAR NOT NULL, description TEXT NOT NULL, icon VARCHAR, -- emoji or icon identifier color_hex VARCHAR NOT NULL, -- e.g., #2E7D32 (green) trustworthiness_rank INT, -- 1 (highest) to 7 (lowest/unknown) created_at TIMESTAMP DEFAULT NOW());-- Every observation has exactly one confidence tierALTER TABLE standardized_observations ADD COLUMN confidence_tier confidence_tier DEFAULT 'UNKNOWN';ALTER TABLE derived_observations ADD COLUMN confidence_tier confidence_tier DEFAULT 'MODELED_DERIVED';-- Missing data states: why is this observation unavailable?CREATE TYPE missing_data_reason AS ENUM ( 'NO_AUTHORITATIVE_DATASET', -- Source doesn't exist 'NOT_YET_PUBLISHED', -- Source exists but data not public 'HISTORICAL_DATA_UNAVAILABLE', -- Pre-dates data collection 'METHODOLOGICAL_BREAK', -- Definition changed; not comparable 'SOURCE_DISCONTINUED', -- Source stopped reporting this metric 'DATA_SUPPRESSION', -- Withheld for privacy/security 'COLLECTION_NOT_STARTED', -- Metric not tracked until later year 'UNKNOWN_REASON', -- Unknown why missing 'PENDING_RETRIEVAL' -- In pipeline; will be available soon);CREATE TABLE missing_data_observations ( missing_data_observation_id UUID PRIMARY KEY, metric_id UUID NOT NULL REFERENCES metrics(metric_id), observation_date DATE NOT NULL, missing_reason missing_data_reason NOT NULL, explanation TEXT NOT NULL, -- Human-readable reason earliest_available_date DATE, -- When data becomes available (if known) source_notes VARCHAR, -- Notes from source agency created_at TIMESTAMP DEFAULT NOW(), created_by VARCHAR);Tier Classification Rules:pythondef classify_confidence_tier( source: Source, retrieval_method: str, data_type: str, is_derived: bool) -> ConfidenceTier: """ Determine confidence tier based on observable characteristics. """ # If derived from other observations, always modeled if is_derived: return ConfidenceTier.MODELED_DERIVED # Official government agency measurement if source.agency_type == 'FEDERAL' and data_type == 'DIRECT_MEASUREMENT': return ConfidenceTier.OFFICIAL_MEASUREMENT # From government admin records (not survey) if source.agency_type == 'FEDERAL' and data_type == 'ADMINISTRATIVE_RECORD': return ConfidenceTier.ADMINISTRATIVE_RECORD # Government statistical survey (BLS CPS, Census ACS, etc.) if source.agency_type == 'FEDERAL' and data_type == 'STATISTICAL_SURVEY': return ConfidenceTier.SURVEY_ESTIMATE # Reconstructed from historical documents (Census tabulations, old reports) if source.data_source_vintage < 1945 and data_type == 'RECONSTRUCTED': return ConfidenceTier.HISTORICAL_RECONSTRUCTION # Published academic research if source.agency_type == 'ACADEMIC': return ConfidenceTier.ACADEMIC_ESTIMATE # Cannot classify return ConfidenceTier.UNKNOWNMissing Data Classification Example:pythondef classify_missing_data( metric_id: UUID, observation_date: date, source: Source) -> MissingDataObservation: """ Determine why a metric is unavailable for a date. """ # Example: Unemployment rate Jan 1890 # BLS Current Employment Statistics started 1945 if metric_id == METRIC_UNEMPLOYMENT_RATE and observation_date < date(1945, 1, 1): if source.collection_start_date == date(1945, 1, 1): return MissingDataObservation( metric_id=metric_id, observation_date=observation_date, missing_reason=MissingDataReason.HISTORICAL_DATA_UNAVAILABLE, explanation="Bureau of Labor Statistics Current Employment Statistics " "series began in January 1945. Data for earlier periods " "unavailable from this source.", earliest_available_date=date(1945, 1, 1), source_notes="See BLS historical documentation for earlier estimates " "from alternative academic sources." ) # Example: Farm count (Census of Agriculture) # Conducted every 5 years; missing in off-years if metric_id == METRIC_FARM_COUNT and observation_date.year % 5 != 0: return MissingDataObservation( metric_id=metric_id, observation_date=observation_date, missing_reason=MissingDataReason.NO_AUTHORITATIVE_DATASET, explanation="USDA Census of Agriculture is conducted decennially " "(every 10 years). No authoritative annual farm count exists. " "NASS provides annual estimates; see domain documentation.", earliest_available_date=None, source_notes="Census conducted in years ending in 2. Next: 2024." )Frontend Confidence Tier Component:tsx// components/confidence/ConfidenceBadge.tsxconst TIER_METADATA = { OFFICIAL_MEASUREMENT: { label: 'Official Measurement', icon: '✓', color: 'bg-green-700', description: 'Direct measurement by authoritative government agency' }, ADMINISTRATIVE_RECORD: { label: 'Administrative Record', icon: ' ', color: 'bg-green-600', description: 'Derived from government administrative processes' }, SURVEY_ESTIMATE: { label: 'Survey Estimate', icon: ' ', color: 'bg-blue-600', description: 'Sampled government survey (e.g., Current Population Survey)' }, HISTORICAL_RECONSTRUCTION: { label: 'Historical Reconstruction', icon: ' ', color: 'bg-amber-600', description: 'Reconstructed from historical documents' }, ACADEMIC_ESTIMATE: { label: 'Academic Estimate', icon: ' ', color: 'bg-purple-600', description: 'Published peer-reviewed scholarly research' }, MODELED_DERIVED: { label: 'Modeled/Derived', icon: ' ', color: 'bg-gray-600', description: 'Calculated from other tracked observations' }, UNKNOWN: { label: 'Unknown', icon: '?', color: 'bg-gray-400', description: 'Confidence classification cannot be determined' }};export function ConfidenceBadge({ tier }: Props) { const meta = TIER_METADATA[tier]; return ( <Tooltip title={meta.description}> <span className={`confidence-badge ${meta.color}`}> {meta.icon} {meta.label} </span> </Tooltip> );}// components/missing-data/MissingDataDisplay.tsxexport function MissingDataDisplay({ missingData }: Props) { return ( <div className="missing-data-panel alert alert-info"> <div className="header"> <span className="icon"> </span> <strong>Data Unavailable</strong> </div> <p className="reason">{missingData.explanation}</p> {missingData.earliest_available_date && ( <p className="note"> Data available from: <strong>{missingData.earliest_available_date.toLocaleDateString()}</strong> </p> )} {missingData.source_notes && ( <details className="source-notes"> <summary>Source Notes</summary> <p>{missingData.source_notes}</p> </details> )} </div> );}Chart Rendering with Confidence Tiers:tsx// components/charts/TimeSeriesChart.tsxexport function TimeSeriesChart({ metric, observations }: Props) { // Group observations by confidence tier const seriesByTier = groupBy(observations, 'confidence_tier'); const echartOption = { xAxis: { type: 'time' }, yAxis: { type: 'value' }, series: Object.entries(seriesByTier).map(([tier, data]) => ({ name: TIER_METADATA[tier].label, type: 'line', data: data.map(obs => [obs.observation_date, obs.value]), lineStyle: { // Official data: solid, thick, green // Survey data: dashed, blue // Modeled data: dotted, gray ...LINE_STYLES[tier] }, itemStyle: { color: TIER_METADATA[tier].color }, // Show tooltip with confidence info tooltip: { formatter: (params) => ` <strong>${metric.name}</strong><br/> Date: ${params[0].axisValue.toLocaleDateString()}<br/> Value: ${params[0].value[1]}<br/> Confidence: ${TIER_METADATA[tier].label}<br/> <small>${TIER_METADATA[tier].description}</small> ` } })), // Critical: show legend so users know which series is which tier legend: { show: true }, // Grid: show missing data gaps as white space (never interpolated) grid: { show: true } }; return <ECharts option={echartOption} />;}Consequences:  Positive:Users instantly understand data certaintyMissing data is visible, not hiddenVisual distinction prevents misuse (e.g., treating survey estimates as official measurements)Supports comparative analysis ("compare farm income [Official] to fertilizer cost [survey]")  Negative:Requires manual tier classification for historical/academic data (mitigated: guidelines + peer review)More database fields (negligible storage cost)API Response Example:json{ "metric_id": "unemployment_rate_total", "observation_date": "2023-06", "value": 3.8, "units": "percent", "confidence_tier": "SURVEY_ESTIMATE", "confidence_metadata": { "display_name": "Survey Estimate", "trustworthiness_rank": 3, "description": "Sampled government survey" }, "provenance": { ... }}Related ADRs: ADR-002 (storage), ADR-004 (provenance)Summary: ADR-001 through ADR-005ADRDecisionKey Constraint001PostgreSQL (operational) + DuckDB (analytics)Two databases; analytically separate but governed together002Three-tier storage (Raw / Standardized / Derived)Raw is immutable; transformations versioned; derivations labeled003Source-specific connectors (SourceConnector base + 15+ implementations)Each source API isolated; independently testable; versioned004Full provenance tracking (Observation → Metric → Dataset → Series → Agency → Retrieval → Transformation → Visualization)Every number traceable; reproducible; audit trail permanent005Mandatory confidence tiers + explicit missing-data statesNever zero for missing; always labeled; distinct visual treatmentDeliverables Summary  ADR-001 through ADR-005 approved and complete.These ADRs operationalize the Master Project Plan's core governance principles:§6 (Raw Data Preservation) → ADR-002§20 (Provenance & Data Lineage) → ADR-004§18 (Confidence Classification) → ADR-005§28–31 (Data Ingestion, Validation, Governance) → ADR-003